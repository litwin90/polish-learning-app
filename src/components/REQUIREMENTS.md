# Требования к компонентам приложения

## Компонент FlashCard

### Текущий функционал

1. **Отображение карточки**
   - Карточка отображает польское слово на лицевой стороне
   - При клике карточка переворачивается и показывает русский перевод
   - Отображаются примеры использования слова (если есть)
   - Карточка имеет анимацию переворота

2. **Режимы обучения**
   - Поддерживает два режима: "pl-to-ru" (польский → русский) и "ru-to-pl" (русский → польский)
   - В зависимости от режима отображаются соответствующие кнопки для отметки уровня знания

3. **Управление уровнем знания**
   - Уровень 0: Не знаю (knowsPlToRu: false, knowsRuToPl: false)
   - Уровень 1: Знаю PL→RU (knowsPlToRu: true, knowsRuToPl: false)
   - Уровень 2: Знаю оба направления (knowsPlToRu: true, knowsRuToPl: true)
   - Кнопки для установки уровня отображаются только после переворота карточки

4. **Навигация**
   - После установки уровня автоматически переходит к следующей карточке
   - Отображается текущий уровень слова

### Новый функционал

1. **Удаление категории из карточки**
   - Категория (category) больше не отображается на карточке в режиме изучения
   - Категория остается в данных, но скрыта в UI карточки

2. **Свайпы для навигации (touch-события)**
   - Свайп влево = следующая карточка (onNext)
   - Свайп вправо = предыдущая карточка (onPrevious, если доступно)
   - Свайпы работают в любом состоянии карточки (перевернута или нет)
   - Минимальное расстояние свайпа: 50px
   - Поддержка только touch-событий (мобильные устройства)
   - Карточка слегка смещается в направлении свайпа во время движения (визуальная обратная связь)
   - Обратная сторона карточки использует нейтральный цвет (from-indigo-500 to-blue-600)
   - Лицевая сторона карточки использует цвет (from-primary-500 to-purple-600)

3. **Статус "требует проверки" (needsReview)**
   - Кнопка для переключения статуса needsReview отображается после переворота карточки
   - Визуальная индикация статуса: желтый баннер "⚠️ Требует проверки" над карточкой, если needsReview = true
   - Кнопка меняет цвет в зависимости от статуса:
     - Активный статус: желтый фон (bg-yellow-500)
     - Неактивный статус: серый фон (bg-gray-300)

4. **Кнопка "Пропустить"**
   - Кнопка "Пропустить" позволяет перейти к следующей карточке без изменения статуса знания
   - Отображается после переворота карточки только на широких экранах (md и выше)
   - Оранжевый цвет (bg-orange-500)

5. **Маленькие кнопки для малых экранов**
   - На малых экранах (меньше md) отображаются маленькие круглые кнопки справа от карточки
   - Кнопки расположены вертикально, удобны для нажатия большим пальцем правой руки
   - Кнопки отображаются только когда карточка перевернута (isFlipped = true)
   - Кнопки:
     - "0" (серая) - Не знаю (устанавливает knowsPlToRu = false, knowsRuToPl = false)
     - "✓" (синяя/фиолетовая) - Знаю (в зависимости от режима: PL→RU или RU→PL)
     - "?" (оранжевая/серая) - Сомневаюсь (переключает isUnsure)
   - Кнопки скрыты на широких экранах (md и выше), где отображаются обычные кнопки

6. **Статус "сомневаюсь" (isUnsure)**
   - Новое поле isUnsure для пометки слов, в которых пользователь сомневается
   - Визуальная индикация: оранжевый баннер "🤔 Сомневаюсь" над карточкой, если isUnsure = true
   - Кнопка для переключения статуса отображается в маленьких кнопках справа (только для малых экранов)
   - Кнопка меняет цвет в зависимости от статуса:
     - Активный статус: оранжевый фон (bg-orange-500)
     - Неактивный статус: серый фон (bg-gray-300)

### Тестовые сценарии

1. **Свайпы для навигации**
   - Свайп влево должен перейти к следующей карточке (onNext)
   - Свайп вправо должен перейти к предыдущей карточке (onPrevious), если доступно
   - Свайпы должны работать в любом состоянии карточки (перевернута или нет)
   - Свайпы должны игнорироваться, если расстояние меньше 50px
   - Карточка должна визуально смещаться в направлении свайпа во время движения

2. **Статус needsReview**
   - Клик по кнопке "Требует проверки" должен переключать статус needsReview
   - Визуальная индикация должна отображаться только когда needsReview = true
   - Статус должен сохраняться в базе данных

3. **Кнопка "Пропустить"**
   - Клик по кнопке "Пропустить" должен перейти к следующей карточке без изменения статуса
   - Статус знания слова не должен изменяться

4. **Категория**
   - Категория не должна отображаться на карточке

5. **Маленькие кнопки для малых экранов**
   - Кнопки должны отображаться только на экранах меньше md
   - Кнопки должны быть скрыты на широких экранах (md и выше)
   - Кнопка "0" должна устанавливать knowsPlToRu = false, knowsRuToPl = false
   - Кнопка "✓" должна устанавливать соответствующий уровень в зависимости от режима
   - Кнопка "?" должна переключать статус isUnsure

6. **Статус "сомневаюсь"**
   - Клик по кнопке "?" должен переключать статус isUnsure
   - Визуальная индикация должна отображаться только когда isUnsure = true
   - Статус должен сохраняться в базе данных

## Компонент WordList

### Текущий функционал

1. **Отображение списка слов**
   - Отображает все слова в виде списка
   - Показывает польское слово, русский перевод, примеры, категорию
   - Отображает текущий уровень знания каждого слова

2. **Фильтрация по уровню знания**
   - Фильтр по уровню 0, 1, 2
   - Можно выбрать несколько уровней одновременно
   - Показывает количество отфильтрованных слов

3. **Сортировка**
   - По уровню знания
   - По дате последнего просмотра
   - Возможность изменения порядка (по возрастанию/убыванию)

4. **Резервное копирование**
   - Экспорт данных в JSON файл
   - Импорт данных из JSON файла

5. **Запуск обучения**
   - Кнопка "Начать изучение" запускает режим обучения

### Новый функционал

1. **Фильтры по уровню языка (CEFR)**
   - Фильтр по уровням: A1, A2, B1, B2, C1, C2
   - Можно выбрать несколько уровней одновременно
   - Слова без уровня языка исключаются из результатов фильтрации
   - Фильтр работает совместно с фильтром по уровню знания

2. **Статус "требует проверки" (needsReview)**
   - Визуальная индикация в списке: желтый бейдж "⚠️ Требует проверки" для слов с needsReview = true
   - Кнопка для переключения статуса needsReview для каждого слова
   - Кнопка меняет цвет в зависимости от статуса

3. **Отображение уровня языка**
   - Уровень языка (A1-C2) отображается как синий бейдж в списке слов

4. **Применение фильтров при запуске обучения**
   - При нажатии "Начать изучение" передаются активные фильтры (по уровню знания и уровню языка)
   - Фильтры применяются к словам перед запуском режима обучения
   - Если после применения фильтров нет слов, показывается предупреждение

### Тестовые сценарии

1. **Фильтры по уровню языка**
   - Фильтр должен показывать только слова с выбранными уровнями языка
   - Слова без уровня должны исключаться при активном фильтре
   - Комбинация фильтров по уровню знания и уровню языка должна работать корректно

2. **Статус needsReview в списке**
   - Визуальная индикация должна отображаться для слов с needsReview = true
   - Клик по кнопке должен переключать статус
   - Статус должен сохраняться в базе данных

3. **Применение фильтров при обучении**
   - При запуске обучения должны применяться активные фильтры
   - Если нет слов после фильтрации, должно показываться предупреждение
   - Слова должны перемешиваться после применения фильтров

4. **Выделение слов, требующих проверки**
   - Слова с needsReview = true выделяются желтым фоном (bg-yellow-50) и желтой рамкой (border-yellow-300)
   - Выделение видно в списке слов для быстрой идентификации

5. **Фильтр по статусу проверки**
   - Фильтр с тремя опциями: "Все", "Требуют проверки", "Не требуют проверки"
   - Радио-кнопки для выбора одного из вариантов
   - Фильтр применяется совместно с другими фильтрами

6. **Сохранение фильтров и сортировок в localStorage**
   - Фильтры и сортировки сохраняются в localStorage при изменении
   - Ключ хранилища: `wordListFilters_v{version}` (например, `wordListFilters_v1.1.0`)
   - При изменении версии данных (breaking changes) настройки автоматически сбрасываются
   - Сохраняются: filterLevels, filterLanguageLevels, filterNeedsReview, sortBy, sortOrder
   - Настройки загружаются при монтировании компонента

7. **Кнопка "Знаю PL" в таблице слов**
   - Кнопка отображается только для слов, где knowsPlToRu = false
   - При нажатии устанавливает knowsPlToRu = true (подтверждение знания перевода с польского на русский)
   - Сохраняет текущее значение knowsRuToPl
   - После нажатия кнопка скрывается (так как knowsPlToRu становится true)
   - Обновляет статистику после изменения

### Тестовые сценарии для новых функций

1. **Сохранение фильтров в localStorage**
   - Фильтры должны сохраняться при изменении
   - При перезагрузке страницы фильтры должны восстанавливаться
   - При изменении версии данных фильтры должны сбрасываться

2. **Кнопка "Знаю PL"**
   - Кнопка должна отображаться только для слов с knowsPlToRu = false
   - При нажатии должен устанавливаться knowsPlToRu = true
   - Статистика должна обновляться после нажатия
   - Кнопка должна скрываться после нажатия

## Компонент WordForm

### Текущий функционал

1. **Форма добавления/редактирования слова**
   - Поля: польское слово, русский перевод, примеры (PL и RU), категория
   - Валидация обязательных полей
   - Сохранение слова в базу данных

### Тестовые сценарии

1. **Валидация формы**
   - Поля "польское слово" и "русский перевод" обязательны
   - Форма не должна отправляться без обязательных полей

